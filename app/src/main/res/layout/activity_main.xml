<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/title_judul_main"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:background="#EB4145"
            android:gravity="center"
            android:text="HASIL REKOMENDASI"
            android:textColor="@color/colorWhite"
            android:textSize="22sp"
            android:textStyle="bold" />


        <androidx.recyclerview.widget.RecyclerView
            android:layout_below="@id/title_judul_main"
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="6dp"
            app:layout_constraintTop_toBottomOf="@+id/title_judul_main">

        </androidx.recyclerview.widget.RecyclerView>

        <TextView
            android:layout_below="@id/recyclerView"
            android:id="@+id/textview_keterangan"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="6dp"
            android:layout_marginBottom="10dp"
            android:background="#FFB8B9"
            android:padding="3dp"
            android:text="@string/rekomendasi"
            android:textSize="14sp"
            app:layout_constraintTop_toBottomOf="@+id/recyclerView" />

    </RelativeLayout>

</ScrollView>